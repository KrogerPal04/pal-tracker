---
applications:
- name: pal-tracker
  path: build/libs/pal-tracker.jar
  host: krogerpal04-pal-tracker
  buildpack: java_buildpack_offline
  services:
  - tracker-database
  env:
    SECURITY_FORCE_HTTPS: true